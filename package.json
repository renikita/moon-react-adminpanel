{
  "name": "moon-react-adminpanel",
  "version": "0.1.0",
  "homepage": "https://renikita.github.io/moon-react-adminpanel",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "react-router-dom": "^6.27.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build"
  }
}
